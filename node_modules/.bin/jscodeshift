#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../5to6-codemod/node_modules/jscodeshift/bin/jscodeshift.js" "$@"
  ret=$?
else 
  node  "$basedir/../5to6-codemod/node_modules/jscodeshift/bin/jscodeshift.js" "$@"
  ret=$?
fi
exit $ret
